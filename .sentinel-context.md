# .sentinel-context.md

## Stack & Frameworks
- **Language**: Python
- **Runtime**: Python 3.x (inferred)
- **Testing**: Standard library unittest/pytest (to be confirmed)
- **Build/Package**: setuptools (likely)

## Critical Paths
- `test.py` - Core test logic; review for:
  - Test coverage and assertions
  - Mocking and side effects
  - Edge cases and error handling

## Patterns & Conventions
- Minimal project structure (early stage)
- Single test file pattern
- Standard Python naming conventions expected

## Skip During Review
- `.git/`, `.gitignore`, `.env` - VCS and environment files
- `*.pyc`, `__pycache__/`, `.pytest_cache/` - Generated bytecode
- `*.egg-info/`, `dist/`, `build/` - Build artifacts
- `*.md` (documentation) - Use only for context
- `.sentinel-context.md` - This file itself

## Review Focus
Primary: `test.py` logic and coverage
Secondary: `README.md` accuracy with implementation